#include <iostream>
using namespace std;
int main ()
{
    int a=5,b=7;
    cout<<"the operation is a&b is "<<(a&b)<<endl;
    cout<<"the operation is a|b is "<<(a|b)<<endl;
    cout<<"the operation is a^b is "<<(a^b)<<endl;
    cout<<"the operation is ~a is "<<(~a)<<endl;
    return 0;
}